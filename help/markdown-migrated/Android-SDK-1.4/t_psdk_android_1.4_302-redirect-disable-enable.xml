<?xml version="1.0"?>
<!DOCTYPE task PUBLIC "-//OASIS//DTD DITA Task//EN" "task.dtd">
<task id="task_302-redirect-disable-enable"> 
  <title>Disable or enable 302 redirect optimization</title> 
  <shortdesc>Use the 
	 <codeph>useRedirectedUrl</codeph> property to turn 302 redirect on (true)
	 or off (false). 
  </shortdesc> 
  <taskbody> 
	 <example id="example_B886777252B745AAB48B1FCC42C97A25">For example:
      <codeblock outputclass="syntax java">// Set useRedirectedUrl property to false
NetworkConfiguration networkConfiguration = new NetworkConfiguration();
networkConfiguration.setUseRedirectedUrl(false);

//Set NetworkConfiguration as Metadata:
MetadataNode resourceMetadata = new MetadataNode(); 
resourceMetadata.setNode(DefaultMetadataKeys.NETWORK_CONFIGURATION.getValue(), 
                         networkConfiguration);

//Call MediaResource.createFromURL to set the metadata:
MediaResource resource = MediaResource.createFromURL(url, resourceMetadata);
 
//Load the resource
mediaPlayer.replaceCurrentItem(resource);</codeblock>
    </example> 
  </taskbody> 
</task> 
