<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE concept PUBLIC "-//OASIS//DTD DITA Concept//EN" "concept.dtd">
<concept id="c_psdk_content-resolvers-for-ad-deletion-android"> 
  <title>Content resolvers for ad deletion / replacement</title> 
  <shortdesc>You can use multiple content resolvers to handle different timeline
	 operations.
  </shortdesc> 
  <conbody> 
	 <p>

		<codeblock outputclass="syntax java">public List&lt;ContentResolver> retrieveResolvers(MediaPlayerItem item) {
    List&lt;ContentResolver> resolvers = new ArrayList&lt;ContentResolver>();
    MediaPlayerItemConfig itemConfig = item.getConfig();
    if(itemConfig) {
        CustomRangeMetadata customRanges = itemConfig.getCustomRangeMetadata();
        if (customRanges) {
            List&lt;ReplaceTimeRange> timeRanges = customRanges.getTimeRangeList();

            if (timeRanges &amp;&amp; timeRanges.size() > 0) {
                //CustomRangeResolver is activated by the presence of CustomRanges
                resolvers.add(new CustomRangeResolver());
            }
        }
        AdvertisingMetadata metadata = itemConfig.getAdvertisingMetadata();
        if (metadata) {
            if (metadata instanceOf AuditudeSettings) 
            resolvers.add(new AuditudeResolver(getContext());                                     
        }
    }
    //Add your custom resolver if any
    resolvers.add(MyOpportunityGenerator(item));
    return resolvers;
}
</codeblock>
 
	 </p> 
  </conbody> 
</concept> 
