<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE task PUBLIC "-//OASIS//DTD DITA Task//EN" "task.dtd">
<task id="t_psdk_va_chapter-support"> 
  <title>Implement chapter support</title> 
  <shortdesc/> 
  <taskbody> 
	 <context id="context_ED6506B5BE3D413C9CEA6BC89DC477EE"> 
		<p>You can define and track <i>custom</i> chapters for video tracking in <ph
					conkeyref="phrases/primetime-sdk-name"/>-based applications.  </p>
			<p>Custom chapters are managed by the application, and are based on CMS data or on
				another way that the application uses to define chapters. </p>
			<note type="warning">Default chapters are not supported in the 2.5 Android <ph
					conkeyref="phrases/primetime-sdk-name"/>.</note> 
	 </context> 
	 <steps id="steps_205AE03EDE6646378DD8647732504111"> 
		<step id="step_BD7419B3A2BC4D8F8EBB7B19B4F86F6C">
				<cmd>Define and track custom chapters. </cmd>
				<info>
					<codeblock outputclass="syntax java">// First, enable chapter tracking by setting  
// enableChapterTracking to true:

vaMetadata.enableChapterTracking(true);
// For custom chapter definitions, provide 
// an array list of chapters through the metadata.
// For example: 3 chapters of 60 second duration each

List&lt;VideoAnalyticsChapterData> chapters = 
  new ArrayList&lt;VideoAnalyticsChapterData>();

Int chapterDuration = 60;
for (var i = 0; i &lt; 3; i++) {
    VideoAnalyticsChapterData chapterData = 
      new VideoAnalyticsChapterData(i * chapterDuration, (i + 1) * chapterDuration); 
    chapterData.setName("chapter_" + (i+1));
    chapters.add(chapterData);
}

vaMetadata.setChapters(chapters);
</codeblock>
				</info>
			</step> 
	 </steps> 
  </taskbody> 
</task> 
