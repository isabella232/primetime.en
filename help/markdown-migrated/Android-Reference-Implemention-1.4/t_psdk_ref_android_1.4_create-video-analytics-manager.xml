<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE task PUBLIC "-//OASIS//DTD DITA Task//EN" "task.dtd">
<task id="t_psdk_ref_create-video-analytics-manager">
    <title>Create the Video Analytics Manager</title>
    <shortdesc/>
    <taskbody>
        <context>
            <p>A new manager class (<codeph>VAManager</codeph>) has been added to the Android
                Reference Implementation. <codeph>VAManager</codeph> simply creates and destroys an
                instance of the <codeph>VideoHeartbeat</codeph> class. The reference implementation
                creates a <codeph>VAManager</codeph> instance when a new
                    <codeph>MediaPlayer</codeph> is created, and destroys that instance when the
                    <codeph>MediaPlayer</codeph> is destroyed. This is implemented in
                    <codeph>PlayerFragment.java</codeph>. </p>
        </context>
        <steps>
            <step>
                <cmd>To create a new Video Analytics Manager: </cmd>
                <info>
                    <codeblock outputclass="syntax java">VAManager vaManager = ManagerFactory.getVAManager(true, config, mediaPlayer); 
vaManager.createVAProvider(getActivity().getApplicationContext()); </codeblock>
                </info>
                <stepresult>
                    <p>The config variable is a concrete implementation of
                            <codeph>IVAConfig</codeph> and contains the runtime
                            <codeph>VideoHeartbeat</codeph> configurations. <note>If the Android
                            application is not configured with an Adobe Analytics account, then
                            video tracking data will not be generated, even if an instance of
                                <codeph>VAManager</codeph> is created and enabled.</note></p>
                </stepresult>
            </step>
        </steps>
    </taskbody>
</task>
