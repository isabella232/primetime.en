<?xml version="1.0"?>
<!DOCTYPE concept PUBLIC "-//OASIS//DTD DITA Concept//EN" "concept.dtd">
<concept id="instant_on-concept"> 
  <title>Instant-on</title> 
  <shortdesc>Instant-on preloads parts of the media on one or more channels.
	 After a user selects or switches channels, the content starts sooner because
	 some of the buffering has already completed. 
  </shortdesc> 
  <conbody> 
	 <p>When your player is in the <codeph>PTMediaPlayerStatusReady</codeph> status, call
				<codeph>prepareToPlay</codeph> to preload and process some of the content for later
			playback. </p> 
	 <p> 
		<note type="tip">If you do not call 
		  <codeph>prepareToPlay</codeph>, calling 
		  <codeph>play</codeph> automatically calls 
		  <codeph>prepareToPlay</codeph> first. The preloading and processing is
		  completed at this time. 
		</note> 
	 </p> 
	 <p><ph
		conkeyref="phrases/primetime-sdk-name"><?xm-replace_text Phrase?></ph>
		completes some or all of the following tasks for 
		<codeph>prepareToPlay</codeph>: 
	 <ul id="ul_0DD20884E01344A1996AECB0095380B6"> 
		<li id="li_E8166927825A4DE79E7F82242271C1F8">If the metadata key 
		  <codeph>kSyncCookiesWithAVAsset</codeph> is set, 
		  <ph
			conkeyref="phrases/primetime-sdk-name"><?xm-replace_text Phrase?></ph>
		  makes one request to the original M3U8 file to synchronize cookies. 
		</li> 
		<li id="li_0EA532827FF44C80B5CA177FE92D59C3">Loads DRM metadata keys. 
		</li> 
		<li id="li_F4A9349C751E413BB43A0748AC88CBD4">Creates and prepares some
		  structures, elements, or assets that are needed for playing content. 
		</li> 
	 </ul> 
	 </p> 
	 <note type="tip">The 
		<codeph>PTMediaPlayer</codeph> and 
		<codeph>PTMediaPlayerItem</codeph> 
		<codeph>prepareToPlay</codeph> methods are equal. To avoid creating a
		separate 
		<codeph>PTMediaPlayer</codeph> instance for each asset, use the 
		<codeph>PTMediaPlayerItem</codeph> method. 
	 </note> 
	 <p>Instant-on helps you launch multiple media player instances, or
		media-player item loader instances, simultaneously in the background and buffer
		video streams in all of these instances. When a user changes the channel, and
		the stream has buffered properly, calling 
		<codeph>play</codeph> on the new channel starts the playback sooner. 
	 </p> 
  </conbody> 
</concept> 
