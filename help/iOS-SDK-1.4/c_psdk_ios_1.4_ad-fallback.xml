<?xml version="1.0"?>
<!DOCTYPE concept PUBLIC "-//OASIS//DTD DITA Concept//EN" "concept.dtd">
<concept id="concept_ad-fallback-vast"> 
  <title>Ad fallback for VAST and VMAP ads</title> 
  <shortdesc> For Digital Video Ad Serving Template (VAST) ads (or creatives)
	 that have the fallback rule enabled, 
	 <ph conkeyref="phrases/primetime-sdk-name"/> treats an ad with an invalid
	 media type as an empty ad and attempts to use fallback ads in its place. You
	 can configure some aspects of fallback behavior. 
  </shortdesc> 
  <conbody> 
	 <p>The VAST/Digital Video Multiple Ad Playlist (VMAP) specification states
		that for ads where VAST fallback is enabled, empty ads automatically trigger
		the use of fallback ads. When a VAST ad is empty, 
		<ph conkeyref="phrases/primetime-sdk-name"/> looks for a valid HLS media
		type replacement among the fallback ads. When a VAST ad in a wrapper has an
		invalid media type, 
		<ph conkeyref="phrases/primetime-sdk-name"/> treats this ad as empty. You
		can configure whether 
		<ph conkeyref="phrases/primetime-sdk-name"/> should do the same for ads
		inline in a VMAP. For more information about the VAST 
		<codeph>fallbackOnNoAd</codeph> feature, see 
		<xref
		 href="http://www.iab.net/guidelines/508676/digitalvideo/vsuite/vast"
		 format="http" scope="external">Digital Video Ad Serving Template (VAST)
		  3.0</xref>. 
	 </p> 
  </conbody> 
</concept> 
