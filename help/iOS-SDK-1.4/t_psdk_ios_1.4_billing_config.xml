<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE task PUBLIC "-//OASIS//DTD DITA Task//EN" "task.dtd">
<task id="task_configure-billing"> 
  <title>Configure billing metrics</title> 
  <shortdesc>If you use the default configuration, there is nothing else you need to do to enable or
		configure billing. If you obtained different configuration parameters from your Adobe
		Enablement representative, use the  <codeph>PTBillingMetricsConfiguration</codeph> class to
		set these parameters up before initializing the media player. </shortdesc> 
  <taskbody> 
	 <context id="context_7029B9467BF146EAAC63F8C3E914FF1F"> 
		<p>Most customers should use the default configuration. 
		</p>
		<note type="important">The configuration you set remains in effect for
		  the life of the media player. Once you initialize the media player, you cannot
		  change the configuration. 
		</note> 
		<p>To configure billing metrics: 
		</p> 
	 </context> 
	 <steps id="steps_4BEA75B052C94A45942D4ABCC35F8EC3"> 
		<step id="step_CE86F0D7F2E34B70A957D02CAF36D4A6"> 
		  <cmd>Enter the following code sample. 
		  </cmd> 
		  <info> 
			 <codeblock platform="ios" outputclass="syntax objective-c">PTBillingMetricsConfiguration *billingConfig = [[[PTBillingMetricsConfiguration alloc] init] autorelease];
billingConfig.enabled = YES;
billingConfig.stdVODBillableDurationMinutes = 60.0;
billingConfig.proVODBillableDurationMinutes = 30.0;
billingConfig.liveBillableDurationMinutes = 15.0;
               
// metadata is the PTMetadata instance set on PTMediaPlayerItem
[metadata setMetadata:billingConfig forKey:PTBillingMetricsConfigurationMetadataKey];</codeblock> 
		  </info> 
		</step> 
	 </steps> 
  </taskbody> 
</task> 
