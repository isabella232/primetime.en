<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE reference PUBLIC "-//OASIS//DTD DITA Reference//EN" "technicalContent/dtd/reference.dtd">
<reference id="timerange-class"> 
  <title>TimeRange class</title> 
  <shortdesc>Custom ad markers allow you to pass a set of <codeph>TimeRange</codeph> specifications
		that represent timeline segments to <ph conkeyref="phrases/primetime-sdk-name"/>. </shortdesc> 
  <refbody> 
	 <section id="section_42EB6D62627A424ABA250E3246EFEFC3"> 
		<p>Each <codeph>TimeRange</codeph> specification in the set represents a segment on the playback
				timeline that is maintained internally by <ph conkeyref="phrases/primetime-sdk-name"
				/> and that must be appropriately marked as an ad-related period. </p> 
		<p>The <codeph>TimeRange</codeph> class is a simple data structure that exposes the start position
				and the end position on the timeline. These two read-only properties abstract the
				idea of a time range in the playback timeline. <note type="tip">Both values are
					expressed in milliseconds. </note>
			</p> 
		<p>Here is a summary of the <codeph>TimeRange</codeph> class:
				<codeblock outputclass="syntax java">public final class TimeRange {
    // the start/end values are provided at construction time
    public static TimeRange createRange(long begin, long duration) {...} 

    // only getters are available
    public long getBegin() {...} 
    public long getEnd() {...} 
    public long getDuration() {...}
}
</codeblock>
			</p> 
	 </section> 
  </refbody> 
</reference> 
