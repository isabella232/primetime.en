<?xml version="1.0"?>
<!DOCTYPE task PUBLIC "-//OASIS//DTD DITA Task//EN" "task.dtd">
<task id="task_fallback-vmap"> 
  <title>Define fallback ad behavior for VMAP inline ads</title> 
  <shortdesc>You can enable fallback when a VMAP inline ad contains an invalid
	 media type. 
  </shortdesc> 
  <taskbody> 
	 <steps id="steps_6E580E09D48B46D8AF34868CE97006A7"> 
		<step id="step_1492971380624EE4A94114DB81E312DA"> 
		  <cmd>Set 
			 <codeph>setFallbackOnInvalidCreativeEnabled</codeph> to 
			 <codeph>true</codeph> to have VMAP fall back when the media type for
			 a linear/inline ad is invalid for HLS. 
		  </cmd> 
		  <info>The default value is 
			 <codeph>false</codeph>. If a linear ad fails because it is has an
			 invalid media type, or because the ad cannot be repackaged, this flag allows 
			 <ph conkeyref="phrases/auditude-name"/> to follow the same fallback
			 behavior as if the ad was an empty VAST wrapper. 
			 <codeblock outputclass="syntax java">AuditudeSettings result = new AuditudeSettings();
result.setFallbackOnInvalidCreative(true);</codeblock>
		  </info> 
		</step> 
	 </steps> 
  </taskbody> 
</task> 
