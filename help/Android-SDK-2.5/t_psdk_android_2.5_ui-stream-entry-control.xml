<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE task PUBLIC "-//OASIS//DTD DITA Task//EN" "technicalContent/dtd/task.dtd">
<task id="enter-a-stream"
 xmlns:ditaarch="http://dita.oasis-open.org/architecture/2005/"> 
  <title>Enter a stream at a specific time</title> 
  <shortdesc>By default, when starting playback, VOD media starts at 0 and live
	 media starts at the client live point 
	 <codeph>(MediaPlayer.LIVE_POINT)</codeph>. You can override the default
	 behavior. 
  </shortdesc> 
  <taskbody> 
	 <steps id="steps_632BBD815BF04B0AAA7D5662A101F16E"> 
		<step id="step_4B4BB8CC782948168216A1D745366B1A"> 
		  <cmd>Pass a position to 
			 <codeph>MediaPlayer.prepareToPlay</codeph>. 
		  </cmd> 
		  <info>
			 <p> <ph conkeyref="phrases/primetime-sdk-name"/> considers the given position to be the starting point for
				the asset, and no seek operation is required. If the position is not inside the
				seekable range, <ph conkeyref="phrases/primetime-sdk-name"/> uses the default position. For more information, see 
				<xref
				 href="t_psdk_android_2.5_media-resource-load.xml#load-a-media-resource"
				 format="dita" scope="local"><?xm-replace_text Load a media resource in the media player?></xref>.
				
			 </p>
			 <p>For example:
						<codeblock outputclass="syntax java">long desiredPostion = // TODO : choose a value;
@Override
public void onStatusChanged(MediaPlayerStatusChangedEvent statusChangedEvent) {  
    switch (statusChangedEvent.getStatus()) {
        case INITIALIZED:
            _mediaPlayer.prepareToPlay(desiredPosition);
            break;
        case PREPARING:
            showBufferingSpinner();
            break;
    }
}</codeblock>
					</p>
		  </info> 
		</step> 
	 </steps> 
  </taskbody> 
</task> 
