<?xml version="1.0"?>
<!DOCTYPE concept PUBLIC "-//OASIS//DTD DITA Concept//EN" "concept.dtd">
<concept id="c_psdk_ref_config-provider"> 
  <title>ConfigProvider</title> 
  <shortdesc>The <codeph>ConfigProvider</codeph> class gets the configuration for the media player.
		You must implement the configuration interface so the feature managers can read the
		configuration information. </shortdesc> 
  <conbody> 
	 <p>You use the <xref
				href="http://help.adobe.com/en_US/primetime/reference_implementation/android/javadoc/com/adobe/primetime/reference/config/ConfigProvider.html"
				scope="external" format="html">ConfigProvider</xref> class to implement the
				<codeph>ICCConfig</codeph>, <codeph>IAAConfig</codeph>,
				<codeph>IPlaybackConfig</codeph>, <codeph>IAdConfig</codeph>, and
				<codeph>IQosConfig</codeph> configuration interfaces so that the feature managers
			can read the configurations. For example, the <codeph>ICCConfig</codeph> is the
			interface for the <codeph>CCManager</codeph> configuration. The configuration files
			receive the configuration parameters from the JSON configuration file. </p> 
	 <p>The <codeph>ConfigProvider.java</codeph> file is an example of Adobe's implementation of the
			configuration interfaces. It reads the settings from <codeph>SharedPreferences</codeph>,
			where the configuration is stored. You can store your configuration in any way that
			works for your organization. The config implementation provides a wrapper for your
			configuration source. </p>
  </conbody> 
</concept> 
