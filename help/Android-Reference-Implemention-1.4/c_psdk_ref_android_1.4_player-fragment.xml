<?xml version="1.0"?>
<!DOCTYPE concept PUBLIC "-//OASIS//DTD DITA Concept//EN" "concept.dtd">
<concept id="c_psdk_ref_player-fragment"> 
  <title>PlayerFragment</title> 
  <shortdesc>The <codeph>PlayerFragment</codeph> class is where you edit the code to create the
		fully-enabled feature managers. </shortdesc> 
  <conbody> 
	 <p>The <codeph>PlayerFragment</codeph> class contains all the UI components such as the
				<codeph>playerFrame</codeph>, <codeph>ControlBar</codeph>,
				<codeph>playerClickableAdFragment</codeph>, and <codeph>adOverlay</codeph>. </p> 
	 <p>It handles the initialization of all these components as well as creating the player, setting
			up the views, creating feature managers for the media player, handling media events such
			as resume, play, and pause and handling the event listeners for
				<codeph>QoSManager</codeph>, <codeph>DRMManager</codeph>,
			<codeph>CCManager</codeph>, <codeph>AAManager</codeph>, <codeph>AdsManager</codeph>,
				<codeph>PlaybackManager</codeph>, and <codeph>EntitlementManager</codeph>. </p> 
	 <p>The XML file that includes the configuration parameters for the <codeph>PlayerFragment</codeph>
			is <filepath>res/layout/fragment_player.xml</filepath>. </p> 
	 <p>Before you create the feature managers you need to create the media player by making sure the
			following code is in the <filepath>PlayerFragment.java</filepath> file:
			<codeblock outputclass="syntax java">private MediaPlayer createMediaPlayer() {
  MediaPlayer mediaPlayer = 
    DefaultMediaPlayer.create(getActivity().getApplicationContext());
  return mediaPlayer;
}</codeblock>
		</p> 
  </conbody> 
</concept> 
