<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE task PUBLIC "-//OASIS//DTD DITA Task//EN" "technicalContent/dtd/task.dtd">
<task id="enter-a-stream"
 xmlns:ditaarch="http://dita.oasis-open.org/architecture/2005/"> 
  <title>Enter a stream at a specific time</title> 
  <shortdesc>By default, when starting playback, VOD media starts at 0
			(<codeph>MediaPlayer.LIVE_POINT</codeph>). You can override the default
		behavior.</shortdesc> 
  <taskbody> 
	 <steps id="steps_632BBD815BF04B0AAA7D5662A101F16E"> 
		<step id="step_3279647E0D08467C8AA602E7EBC6F163" 
		 > 
		  <cmd>Pass a position to 
			 <codeph>MediaPlayer.prepareToPlay</codeph>. 
		  </cmd> 
		  <info> 
			 <p><ph conkeyref="phrases/primetime-sdk-name"/> considers the given
				position to be the starting point for the asset. No seek operation is required.
				If the position is not inside the seekable range, 
				<ph conkeyref="phrases/primetime-sdk-name"/> uses the default
				position. 
			 </p> 
			 <p>For example:
						<codeblock outputclass="syntax java">long desiredPostion = //TODO : choose a value;
@Override
public void onStateChanged(MediaPlayer.PlayerState state, MediaPlayerNotification notification) {
    switch (state) {
        case INITIALIZED:
            _mediaPlayer.prepareToPlay(desiredPosition);
            break;
            case PREPARING:
            showBufferingSpinner();
            break;
        ...
    }
}
</codeblock></p> 
		  </info> 
		</step> 
	 </steps> 
  </taskbody> 
</task> 
