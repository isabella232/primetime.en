<?xml version="1.0"?>
<!DOCTYPE task PUBLIC "-//OASIS//DTD DITA Task//EN" "task.dtd">
<task id="task_impl_cust_time_updates"> 
  <title>Implement custom time updates</title> 
  <shortdesc>In some analytics implementations, the client application might
	 want to provide a different playhead position than the position  reported by the 
  	<ph conkeyref="phrases/primetime-sdk-name"/> <codeph>localTime</codeph> value. 
  	For example, during a linear stream playback, each program’s playhead can be provided 
  	relative to its start time. 
  </shortdesc> 
  <taskbody> 
	 <context> 
		<note type="tip"
				>Override this method only if you want to provide a playhead position other than the default position. </note> 
	 </context> 
	 <steps> 
		<step> 
		  <cmd>To override the default playhead position:</cmd> 
		  <info> 
			 <codeblock outputclass="syntax java">vaMetadata.setCurrentTimeUpdateBlock(new VideoAnalyticsMetadata.CurrentTimeUpdateBlock() {
    @Override
    public long call() {
        long range = 500L;
        Random r = new Random()
        return (long)(r.nextDouble()*range);
    }
});</codeblock> 
			 <note type="important">The values in this code snippet are only
				samples. You need to use different values for your custom playhead position. 
			 </note>
		  </info> 
		</step> 
	 </steps> 
  </taskbody> 
</task> 
